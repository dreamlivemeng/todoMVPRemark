<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.junze.mvp.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.junze.mvp.demo.tasks.TasksActivity"
            android:theme="@style/AppTheme.OverlapSystemBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".addedittask.AddEditTaskActivity"
            android:theme="@style/Base.AppTheme"></activity>
        <activity
            android:name=".taskdetails.TaskDetailActivity"
            android:theme="@style/Base.AppTheme"></activity>
        <activity android:name=".statistics.StatisticsActivity"></activity>
    </application>

</manifest>